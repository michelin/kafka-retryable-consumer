<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>com.michelin.kafka</groupId>
        <artifactId>kafka-retryable-consumer-examples</artifactId>
        <version>1.2.4-SNAPSHOT</version>
    </parent>

    <artifactId>example-spring-boot</artifactId>

    <name>example-spring-boot</name>
    <description>Spring Boot examples of the retryable consumer starter, one capability per class.</description>

    <dependencyManagement>
        <dependencies>
            <!--
                The Spring Boot BOM imported below manages the whole Apache Kafka family, on a version older than the
                one this project targets. Left alone it silently downgrades the broker internals pulled by the embedded
                cluster, and the integration tests fail at runtime on a NoClassDefFoundError rather than at build time.

                These entries come first on purpose: the first declaration wins in dependencyManagement. Only the
                artifacts the BOM actually downgrades are listed, the others already resolve to the right version.
            -->
            <dependency>
                <groupId>org.apache.kafka</groupId>
                <artifactId>kafka-metadata</artifactId>
                <version>${kafka.apache.version}</version>
            </dependency>

            <dependency>
                <groupId>org.apache.kafka</groupId>
                <artifactId>kafka-raft</artifactId>
                <version>${kafka.apache.version}</version>
            </dependency>

            <dependency>
                <groupId>org.apache.kafka</groupId>
                <artifactId>kafka-server</artifactId>
                <version>${kafka.apache.version}</version>
            </dependency>

            <dependency>
                <groupId>org.apache.kafka</groupId>
                <artifactId>kafka-server-common</artifactId>
                <version>${kafka.apache.version}</version>
            </dependency>

            <dependency>
                <groupId>org.apache.kafka</groupId>
                <artifactId>kafka-storage</artifactId>
                <version>${kafka.apache.version}</version>
            </dependency>

            <dependency>
                <groupId>org.apache.kafka</groupId>
                <artifactId>kafka-storage-api</artifactId>
                <version>${kafka.apache.version}</version>
            </dependency>

            <dependency>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-dependencies</artifactId>
                <version>${spring-boot.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <dependencies>
        <dependency>
            <groupId>com.michelin.kafka</groupId>
            <artifactId>retryable-consumer-spring-boot-starter</artifactId>
            <version>${project.version}</version>
        </dependency>

        <!--
            Pinned on the version targeted by the core library: the Spring Boot BOM imported above would otherwise
            downgrade the Kafka client and break the binary compatibility with the core.
        -->
        <dependency>
            <groupId>org.apache.kafka</groupId>
            <artifactId>kafka-clients</artifactId>
            <version>${kafka.apache.version}</version>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter</artifactId>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>
</project>
